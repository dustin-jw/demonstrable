---
order: 18
---

#### Creating a Commit

```sh
# open a file editor to write your commit message
git commit

# write your commit message right in the command line
git commit -m "<commit-message>"

# edit the previous commit (either just the message or files, too)
git commit --amend
```
